# Ignores HTML, TeX, and CSS in GitHub's language calculation because there is a lot of Python code and it isn't being counted correctly.
*.html linguist-vendored
*.css linguist-vendored
*.tex linguist-detectable=false